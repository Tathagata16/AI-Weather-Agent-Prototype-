# description:
  llm calls weather api, uses available tools and returns current weather of a place

# bugs:
  only works for big cities, 
  No UI
  slow response
  sometimes may not return json format
